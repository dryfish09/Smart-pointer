# Smart pointer code example
This repository contains some **basic code example** for a very *peak* feature of C++ 11 or higher that help you using pointer safer.

## Purpose:
This repository just for educational purpose only.

## What's this repository *actually* contains?
Yea, this repository contains `shared_pointer`, `unique_ptr` and `weak_ptr` (it acually weak and peak)

You can view code on `src` directory.
